import React from 'react';
import { Grid, Cell, Card, CardActions, CardTitle, Icon, Button, FABButton } from 'react-mdl';

const MissionView = (props) => (

    <section id={props.id} className="icg-home-mission">

      <h1>Mission</h1>

      <h2>Blah Blah</h2>

    </section>

)

export default MissionView;
